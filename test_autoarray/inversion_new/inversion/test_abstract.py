import numpy as np
from os import path
import pytest

from autoarray import aa_new

from autoarray import exc

directory = path.dirname(path.realpath(__file__))

# def test__linear_obj_all_with_regularization():
#
#     inversion = AbstractInversion(linear_obj_list=)
